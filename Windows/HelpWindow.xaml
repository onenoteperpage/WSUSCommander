<Window x:Class="WSUSCommander.Windows.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WSUSCommander.Windows"
        mc:Ignorable="d"
        Title="Help" Height="480" Width="800">
    <StackPanel>
        <RichTextBox x:Name="HelpRichTextBox"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto" 
                     Height="400"/>
        <!-- Optional height to give space for button -->
        <Button x:Name="btn_CloseWindow"
                Content="Close Help"
                Click="btn_CloseWindow_Click"
                HorizontalAlignment="Right"
                Margin="10"
                Padding="10,4"/>
    </StackPanel>
</Window>
